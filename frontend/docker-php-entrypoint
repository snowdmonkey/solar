#!/bin/sh
set -e

# first arg is `-f` or `--some-option`
if [ "${1#-}" != "$1" ]; then
	set -- apache2-foreground "$@"
fi

python init.py

cp /var/www/html/data/panorama.png /var/www/html/img/panorama.png

exec "$@"
